# 📡 Agent Sync Bus — {{PROJECT_NAME}}

> **This directory has its own git repository** — separate from the project repo.  
> All agent communication, state, and decisions are version-tracked here.

**Runtime Version:** See `RUNTIME_VERSION`  
**Protocol:** D021 + D022 + D023.x + D024 + D031

---

## 🏛️ Authority Model

```
CEO (Top Manager)
    │
    ├── Product scope, priorities, releases
    └── Final decisions
         │
         ▼
Claude (Senior Architect)
    │
    ├── Architecture decisions
    ├── Work order management
    └── Runtime normalization
         │
         ▼
Gemma (QA Lead)
    │
    └── Quality gates
         │
         ▼
Workers (Codex, Gemini, Local-LLM)
    │
    └── Implementation
```

---

## 📁 Directory Structure

```
.sync/
├── RUNTIME_VERSION          ← Version tracking
├── PROTOCOL_DIGEST.md       ← Protocol rules
├── PROTOCOL_DIGEST.hash     ← Protocol hash
├── README.md                ← This file
├── runtime/
│   ├── TREE.yaml            ← Team state
│   ├── boot/                ← Agent snapshots
│   ├── drafts/              ← Worker drafts
│   └── receipts/            ← Compliance receipts
├── work-orders/
│   ├── INDEX.yaml           ← Work order index
│   ├── TEMPLATES/           ← WO templates
│   ├── ACTIVE/              ← In-progress
│   ├── COMPLETED/           ← Finished
│   └── BLOCKED/             ← Blocked
├── agents/                  ← Agent contracts
├── inbox/                   ← Messages TO agents
├── outbox/                  ← Reports FROM agents
├── state/                   ← Legacy checkpoints
├── decisions/               ← Decision log
├── reviews/                 ← Code reviews
├── escalations/             ← CEO escalations
├── standup/                 ← Standup notes
└── releases/                ← Release coordination
```

---

## 🚀 Agent Boot Sequence

```
0. READ  AGENTS.md (project root)
1. READ  runtime/boot/<agent>.boot.yaml
2. PEEK  TREE.yaml tree_version
3. CHECK PROTOCOL_DIGEST.hash
4. CHECK inbox (skip if unread=0)
5. CHECK decisions (skip if watermark current)
6. READ  assigned work orders
7. RESUME from next_action
```

---

## 🛑 Agent Shutdown Sequence

```
1. WRITE  draft snapshot → runtime/drafts/
2. WRITE  session report → outbox/
3. ARCHIVE inbox → inbox/<agent>/_read/
4. OUTPUT Handoff Report
5. COMMIT .sync repo
```

---

## 📋 Quick Reference

| Action | Location |
|--------|----------|
| Read my contract | `agents/<agent>.agent.md` |
| Resume work | `runtime/boot/<agent>.boot.yaml` |
| Check team state | `runtime/TREE.yaml` |
| Read messages | `inbox/<agent>/` |
| Send message | `inbox/<target>/` |
| Write report | `outbox/<agent>/` |
| Request review | `inbox/gemma/` |
| Report completion | `inbox/claude/` |

---

**Generated by stackmind v{{RUNTIME_VERSION}}**
